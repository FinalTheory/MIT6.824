# Prompt For Codex

## Background

- 这是一份MIT 6.8540 分布式系统课程的go代码，其中包含一份完整的raft实现
- 我现在做的工作是基于已有的shard KV和Raft，手工实现一个经典的2PC事务支持


## Requirements

- 只在我明确要求为某个特定部分编写代码时，再开始编码。
- 仅在我的设计框架下完成代码实现的填充；如果感觉框架本身不完善，则中断编码，要求给出更详细的context。
- 不要引入额外的大量辅助代码；如果感觉utils不完善，中断编码并要求指示。
- 不要自作主张地添加大量代码实现。
- 如果某些部分的实现超过100行代码，则暂停编码，要求进一步补全设计。
- 由于是学习目的的代码，忽略所有的panic使用不规范，因为它的目的是直接暴露出问题。
- 由于整个项目状态复杂，每次当我们讨论一些设计思路并且收敛结论之后，帮我把它们都更新总结进 `Design.md`，注意检查是否和已有的item重叠，如果是的话只更新，不要盲目新增条目。
- 类似地，如果我让你帮我二次检查某个逻辑的实现，如果你确认对应部分没有问题，就检查 `Design.md` 中是否存在相应的条目，然后删除掉。
- 有时候我会让你检查大致流程是否正确，此时可以不用提出留空的实现有问题，我会稍后补上。
- 对测试代码，默认采用更直接的写法：如果初始化流程已经保证对象会被成功创建，就不要额外加入防御性的 `nil` 检查。
